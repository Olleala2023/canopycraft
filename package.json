{
  "name": "canopycraft",
  "version": "0.5.1",
  "description": "Расчёт навеса, пристроенного к дому: стропила, обрешётка, прогоны, столбы. Сосна и профильная труба.",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "node tools/build.mjs",
    "test": "node --test tests/*.test.js",
    "dev": "python3 -m http.server 8080",
    "smoke": "node tools/smoke.mjs"
  }
}
