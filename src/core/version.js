/**
 * Версия программы — единственный источник. Её показывают подвал и отчёт,
 * чтобы по распечатке было видно, какой версией посчитан вариант.
 *
 * Совпадение с package.json и верхней записью CHANGELOG.md проверяет
 * tests/version.test.js. Когда поднимать — см. «Версия» в CLAUDE.md.
 */
export const VERSION = '0.10.1';
