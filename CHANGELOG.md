# Изменения

Что менялось от версии к версии. Главное здесь — **изменения, которые сдвигают числа
расчёта**: по версии в отчёте видно, какой программой посчитан вариант, а здесь —
чем она отличается от нынешней. Когда поднимать версию — см. «Версия» в `CLAUDE.md`.

## 0.8.0 — 2026-09-24

**Новое**

- Блок «Стена дома»: ширина стены и расстояние от её левого угла до навеса. Ширина не
  задана — стена по ширине навеса, как у навеса во всю стену вровень с углами дома. Раньше
  в 3D стена всегда выступала за навес на 600 мм с каждой стороны.
- Окна и двери: добавляются кнопками «+ окно» и «+ дверь», размеры и положение правятся в
  панели. Видны в 3D на стене дома и на плане разрывом в линии стены.
- Предупреждение, если столб у стены встал на проём (со счётом шпилек, попавших в него) и
  если обвязка перекрывает верх окна или двери. Проём со столбом на плане красный.

Числа расчёта не меняются.

## 0.7.0 — 2026-09-24

**Новое**

- Вкладка «3D»: навес целиком — стропила, обрешётка, кровля, прогон и обвязка, столбы,
  фундаменты, связи, стена дома. Детали окрашены по коэффициенту U, клик по детали
  открывает её проверки. Вращение, приближение и сдвиг — мышью и пальцами. Геометрия —
  из той же модели, что у расчёта, рисует three.js.

**Внутреннее**

- В сборке three.js (MIT) — `index.html` минифицирован и весит ~750 КБ.

## 0.6.0 — 2026-09-24

**Интерфейс**

- Пояснения к полям — в карточке у кнопки «?»: по нажатию (и на телефоне) или по
  наведению мыши. Под полями больше нет длинных абзацев — панель параметров короче на
  треть (на экране 1710 × 1225 — 2919 → 1788 px). Под полем остались только «живые»
  строки о состоянии расчёта.
- У числовых полей (цены) появилась кнопка «?» — ссылка на статью у них была, но не
  показывалась.

## 0.5.2 — 2026-09-24

**Интерфейс**

- Правая колонка — проверки выбранного элемента, площадка, материалы, цены — на широком
  экране тоже прилипает к верху и прокручивается сама. При выборе другого элемента она
  возвращается к его проверкам.

## 0.5.1 — 2026-09-24

**Интерфейс**

- Рабочее поле на широком экране — по высоте окна, а не до 620 пикселей, и при
  прокрутке длинной панели параметров прилипает к верху: чертёж всё время на виду.
  На телефоне и узком экране — как раньше.

## 0.5.0 — 2026-09-24

**Новое**

- Отменить и вернуть: Ctrl+Z, Ctrl+Y (и Ctrl+Shift+Z), кнопки ↶ ↷ в шапке. Протянутый
  ползунок и перетаскивание отменяются одним шагом; отменяется и «Сбросить». Работает в
  русской раскладке.

**Исправлено**

- Backspace и Delete в поле ввода удаляли выбранное стропило или столб. Теперь в поле они
  стирают текст, а удаляют элемент только вне полей.

**Внутреннее**

- Расчётное ядро и интерфейс разбиты на модули по элементам; поведение не изменилось.
  Забытый при переносе импорт ловят тест и смоук модульной версии `dev.html`.

## 0.4.0 — 2026-09-24

**Числа расчёта изменились**

- Вес деревянной обрешётки на **стальных** стропилах теперь идёт с γf = 1,1, как у
  дерева (табл. 7.1 СП 20), а не с 1,05 от материала стропила. Нагрузка на стальное
  стропило, прогон и столбы растёт примерно на 0,04 % — в запас. Деревянных стропил
  и варианта по умолчанию не касается.

**Проверки**

- Ещё три эталона, посчитанные вручную: стальное стропило, снеговой мешок по схеме Б.8
  от формулы (Б.5) до моментов стропила, крест в ряду с условной силой по (18) СП 16.
  Допуски сверки ужаты до 10⁻⁶ (равномерная нагрузка) и 10⁻³ (снеговой мешок) — так
  эталон и нашёл ошибку с γf обрешётки.

## 0.3.0 — 2026-09-24

**Числа расчёта изменились**

- Момент над опорой больше не проскакивает между точками эпюры. Раньше пик |M| над
  опорой, не попавшей на равномерную сетку, занижался на V·dx/2 — у неразрезных прогонов
  до нескольких процентов, не в запас. U наружного прогона варианта по умолчанию:
  0,189 → 0,193.
- Изменяемая схема — кусок между стыками встык меньше чем на двух опорах — даёт U = ∞ и
  проверку «Изменяемая схема» вместо числа порядка 10¹¹ по вырожденному решению.
  Нагрузка с такого куска больше не теряется из реакций.
- Стропило считается по длине с припуском 100 мм, как в спецификации.

**Отчёт**

- В отчёте — версия программы и ссылка на расчёт: по распечатке вариант открывается
  снова таким, каким его посчитали.

**Проверки**

- Эталонные расчёты: стропило вручную по СП 20, коэффициенты использования и смета
  варианта по умолчанию, вырожденные случаи.
- Палитра: цвета только из токенов, каждый токен во всех темах и в печати.
- Смоук: контраст светлой и тёмной тем, ширина телефона без прокрутки вбок, скриншоты
  вкладок артефактом CI; Chrome на сервере CI ждётся дольше и запускается повторно.
- Правила работы с проектом — `CLAUDE.md`, шаблон PR.

## 0.2 — 2026-09-13 … 2026-09-22

Версия без отдельных выпусков: всё, что было сделано до 0.3.0, сводно.

- Расчёт: стропила, обрешётка, прогон, обвязка у стены, столбы обоих рядов по СП 64 и
  СП 16; МКЭ балки Тимошенко; снег по СП 20 со снеговым мешком по схеме Б.8, ветер.
- Узлы: отрыв стропила от опоры, узел «прогон — столб», база столба, стыки по длине с
  накладками, вкладка «Узлы» с чертежами по числам расчёта.
- Связи: μ столбов из схемы связей, крест в ряду, диагонали в плоскости кровли.
- Фундамент и мороз: блок фундамента, глубина промерзания, пучинистые грунты,
  касательные силы пучения (СП 22), трение о талый грунт (СП 24).
- Подбор: автоподбор сечений и шага, подбор по минимальной стоимости.
- Интерфейс: план, разрез, эпюры, отчёт, смета с ценами, ссылка на расчёт, тёмная тема,
  справка, фоновая музыка.
- Исправления, сдвигавшие числа: снег считался как расчётный при нормативной таблице;
  коэффициенты γc и γR,f трения о талый грунт; при кресте столб годится, только если к
  нему приваривается диагональ; подбор по цене переплачивал за сечение.
